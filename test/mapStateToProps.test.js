import tape from 'tape'
import mapStateToProps from '../src/mapStateToProps'
import toTitleCase from '../src/toTitleCase'
import { sphere } from './__mocks__'

tape('"mapStateToProps" create a new object from a spec full of functions', t => {
  t.end()
})
