const makePropPath = require('./makePropPath')
const areAllFunctions = require('./areAllFunctions')
const getConstructorName = require('./getConstructorName')

module.exports = {
  makePropPath,
  areAllFunctions,
  getConstructorName
}
